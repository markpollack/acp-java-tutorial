{
  "moduleId": "module-08-permissions",
  "displayName": "Module 08: Permissions",
  "timeoutSec": 120,
  "requiredEnv": ["GEMINI_API_KEY"],
  "requiresPackage": false,
  "expectedBehavior": "Handle permission requests from the agent using requestPermissionHandler. When agent requests permission for an operation, display the permission options and auto-approve. Show PermissionOption types (ALLOW_ONCE, ALLOW_ALWAYS, REJECT_ONCE) and the selected outcome."
}
